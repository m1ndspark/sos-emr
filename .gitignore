# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Secrets and credentials (must never be committed per CLAUDE.md)
.env
.env.*
*.key
*.pem
secrets.*
*credentials*
*token*

# Logs / temp
*.log
*.tmp

# Zoho app-definition exports (working input for tools/ds_sync.py, not a repo artifact)
*.ds
